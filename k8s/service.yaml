# Generated by CloudMind
apiVersion: v1
kind: Service
metadata:
  name: via-vai-app
  namespace: cloudmind-staging
  labels:
    app.kubernetes.io/name: via-vai
    app.kubernetes.io/component: app
    app.kubernetes.io/managed-by: cloudmind
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/name: via-vai
    app.kubernetes.io/component: app
  ports:
    - name: http
      port: 3000
      targetPort: 3000

