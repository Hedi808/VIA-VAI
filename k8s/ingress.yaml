# Generated by CloudMind
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: via-vai-ingress
  namespace: cloudmind-staging
  labels:
    app.kubernetes.io/name: via-vai
    app.kubernetes.io/managed-by: cloudmind
  annotations:
    kubernetes.io/description: "CloudMind generated ingress for via-vai"
spec:
  rules:
    - host: VIA-VAI.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: via-vai-app
                port:
                  number: 3000
